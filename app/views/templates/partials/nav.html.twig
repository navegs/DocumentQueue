    <section class="menu-section">
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-12">
                    <div class="navbar-collapse collapse">
                        <ul id="menu-top" class="nav navbar-nav navbar-right">
                            <li><a href="{{ urlFor('home') }}"><span class="glyphicon glyphicon-home"></span> Home</a></li>
                            {% if auth %}
                                <li><a href="{{ urlFor('course') }}"><span class="glyphicon glyphicon-book"></span> Courses</a></li>
                                <li><a href="{{ urlFor('advisor') }}"><span class="glyphicon glyphicon-education"></span> Advisors</a></li>
                                {% if auth.hasRole('ADMIN') %}
                                <li class="dropdown"><a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="glyphicon glyphicon-cog"></span> Administration <span class="caret"></span></a>
                                    <ul class="dropdown-menu">
                                        <li><a href="{{ urlFor('admin.users') }}">Users</a></li>
                                        <li><a href="{{ urlFor('admin.courses') }}">Courses</a></li>
                                    </ul>
                                </li>
                                {% endif %}
                                <li><a href="{{ urlFor('logout') }}"><span class="glyphicon glyphicon-log-out"></span> Logout</a></li>
                            {% else %}
                                <li><a href="{{ urlFor('register') }}"><span class="glyphicon glyphicon-user"></span> Register</a></li>
                                <li><a href="{{ urlFor('login') }}"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>
                            {% endif %}
                        </ul>
                    </div>
                </div>

            </div>
        </div>
    </section>
    <!-- MENU SECTION END-->